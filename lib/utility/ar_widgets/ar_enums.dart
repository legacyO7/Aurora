
enum DISABLE {faustus, threshold, all, none, uninstall}
enum ARMODE {faustus, batteryManager, mainline, mainlineWithoutBatteryManager}
enum CommandStatus { stderr, stdout, stdinp }
enum BuildType { debug, rpm, deb, appimage }