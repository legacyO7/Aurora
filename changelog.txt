
Changelogs:

- optimized terminal op.
- added option for complete uninstallation of aurora from within the package. Since Aurora doesn't expose its binary in the known system paths, package managers might not be able to uninstall the package.
- periodic dependency updates
